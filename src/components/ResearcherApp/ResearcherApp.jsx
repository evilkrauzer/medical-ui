import React, { Component } from 'react';
import { connect } from 'react-redux';
import { mapStateToProps } from './state';

export default class ResearcherApp extends Component {
    static propTypes = {
    };

    render() {
       
        return (
            <div className='test'>
            </div>
        );
    }
}
